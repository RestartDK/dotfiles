return {
  "RestartDK/ghostty-navigator.nvim",
  -- Ghostty native panes are disabled; Herdr is the multiplexer now.
  -- Keep this plugin disabled so vim-tmux-navigator owns Ctrl-h/j/k/l.
  enabled = false,
}
