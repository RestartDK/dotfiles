# Global rules

Starting a non-trivial engineering task → read `~/.agents/skills/dstack/dstack-mode/SKILL.md` in full and apply it: match a playbook, copy its steps verbatim into todos, cite principles with the decision each changed. Casual turns and trivial one-liners skip this. `/dstack` is the explicit entry point.
