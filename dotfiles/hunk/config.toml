# Let Hunk choose a readable light/dark palette from the terminal background.
theme = "auto"

# Keep diff row highlights visible. Set this to true only for fully transparent Hunk surfaces.
transparent_background = false
